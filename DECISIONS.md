# 설계 결정 기록

각 항목은 "무엇을 정했나 / 왜 그렇게 정했나"를 한두 줄로 남긴다.
나중에 이 코드를 설명할 때 그대로 근거로 쓸 수 있는 수준을 목표로 한다.

---

## M1 — 모델 · 로더 · 검증기

**D1. 시각과 기간은 밀리초 정수(`Ms = int64_t`)로 다룬다.**
장비 제어에서 시간 비교는 "같다/이르다"의 판정이 정확해야 한다. 부동소수로 두면 누적 오차 때문에
`start == prev_end + settle` 같은 경계 판정이 흔들린다. 정수로 두면 비교·정렬·해시가 모두 안전하다.

**D2. `Step.start_ms`는 선택 항목이다.**
`validate`는 시각이 채워진 시나리오를 검사하고, M2의 `schedule`은 비어 있는 시각을 채운다.
두 명령이 같은 입력 형식을 공유하므로, 스케줄러 출력을 그대로 검증기에 다시 넣는 왕복 테스트가 가능해진다.

**D3. 로더는 첫 오류에서 멈추지 않고 가능한 모든 오류를 모아 돌려준다.**
입력 파일을 한 번 열어 고칠 때 최대한 많은 문제를 보여주는 편이 실제로 쓰기 편하다.
오류마다 `/steps/2/rate` 같은 JSON 위치를 함께 준다.

**D4. 참조 무결성 검사는 로더의 책임이다.**
자원·동작 id 조회가 실패할 수 있는 지점을 로더 한 곳으로 몰아두면, 이후 단계(검증기·스케줄러)는
"id 조회는 항상 성공한다"고 가정하고 본래 로직에만 집중할 수 있다.

**D5. 검증 규칙은 상속 계층이 아니라 concept + tuple로 묶는다.**
규칙끼리 공유할 상태가 없어 기반 클래스가 값을 못 한다. 규칙 목록이 컴파일 타임에 고정되므로
형태가 어긋난 규칙은 실행이 아니라 컴파일에서 걸린다. 규칙이 자기가 내는 위반 코드(`codes`)를
직접 들고 있어 `ess rules` 출력이 코드와 자동으로 일치한다.

**D6. `PlacedStep`에 `<=>`를 직접 정의해 정렬 기준을 (구간, id)로 고정했다.**
포인터 값이나 입력 순서로 정렬하면 실행할 때마다 위반 보고 순서가 달라질 수 있다.
장비 제어 도구는 같은 입력에 항상 같은 출력을 내야 재현·디버깅이 된다.
입력 순서를 뒤집어도 결과가 같은지 확인하는 테스트를 함께 두었다.

**D7. `cooldown_ms`는 휴지 시간이자 "연속 동작이 끊겼다고 볼 최소 간격"이다.**
연속 동작 누적을 어디서 끊을지 기준이 하나 필요한데, 값을 하나 더 만드는 대신 의미가 같은
`cooldown_ms`를 재사용했다. 대신 `max_continuous_ms > 0`인데 `cooldown_ms`가 0이면
연속 동작 한도가 조용히 무의미해지므로, 이를 로더에서 입력 오류로 막는다.

**D8. `DUTY_EXCEEDED`와 `COOLDOWN_VIOLATION`은 한 규칙에서 함께 낸다.**
둘 다 같은 "연속 구간 누적" 계산에서 나온다. 규칙을 나누면 같은 계산을 두 번 하게 된다.
한 번 보고한 뒤에는 연속 구간을 새로 열어, 하나의 원인이 뒤따르는 동작까지 연쇄 보고되지 않게 했다.

**D9. 배타 관계는 대칭으로 본다.** 한쪽만 `exclusive_with`에 적어도 성립하고, 양쪽이 적어도 한 번만 검사한다.
정렬된 자원 id 쌍으로 중복을 제거한다. 사람이 입력 파일을 쓸 때 양쪽에 다 적어야 하는 실수를 없앤다.

**D10. 같은 자원의 구간 겹침은 `SETTLE_VIOLATION`으로 보고한다.**
안정화 시간이 0이어도 "간격 ≥ 0"이 요구되므로 겹침이 자연스럽게 걸린다. 위반 코드를 하나 더
만들지 않아도 되고, 메시지에서 겹침인지 대기 부족인지 구분해 준다.

**D11. 종료 코드를 0(위반 없음) / 1(위반 발견) / 2(입력 오류)로 나눴다.**
CI나 스크립트가 "검사는 정상 수행됐고 위반이 있다"와 "입력이 잘못됐다"를 구분할 수 있어야 한다.

**D12. `std::format`을 쓴다.** MSVC 19.41(VS 2022, C++20)에서 `<format>` 지원을 확인했다.
대체 구현은 두지 않는다. 다른 컴파일러로 옮길 때 지원 여부를 다시 확인할 것.

---

## M2 — 스케줄러

**D13. 백트래킹하지 않는다.** 위상 순서대로 한 동작씩 시각을 정하고, 한 번 정한 시각은 바꾸지 않는다.
최적해를 찾는 것보다 *제약을 어기지 않는 실행 가능한 해를 항상 같은 방식으로 만들어내는 것*이
장비 제어에서 더 쓸모 있다. 결과가 매번 같아야 재현하고 디버깅할 수 있다.

**D14. 위상 정렬에서 동시에 놓을 수 있는 동작이 여럿이면 입력 순서가 빠른 쪽을 고른다.**
준비 목록을 인덱스 정렬 집합으로 두어 구현했다. 사용자가 JSON에 쓴 순서로 우선순위를 표현할 수
있게 되고, 같은 입력이면 결과가 항상 같아진다.

**D15. 시각을 옮겨서 고칠 수 없는 제약은 배치를 시작하기 전에 걸러낸다.**
동작률 범위 위반과 "동작 하나가 연속 동작 한도보다 긴" 경우가 여기 해당한다.
배치를 끝까지 시도한 뒤 실패를 보고하면 원인이 배치 과정에 묻힌다. 먼저 걸러내면 메시지가
"이건 어떤 배치로도 안 됩니다"로 분명해진다.

**D16. 자원 하나에서는 항상 마지막 동작 뒤에만 배치한다(빈 구간에 끼워 넣지 않는다).**
빈 구간 삽입을 허용하면 더 짧은 스케줄이 나오지만, 연속 동작 누적 계산이 앞뒤로 번져 복잡해진다.
뒤에만 붙이면 누적이 **검증기 `DutyRule`과 똑같은 전방 상태 기계**가 되어, 스케줄러가 만든 결과가
검증을 통과한다는 것이 구조적으로 보장된다. 최적성을 조금 포기하고 정확성과 설명 가능성을 얻었다.

**D17. 배타 자원 회피는 "겹치는 구간의 끝으로 민다"를 겹침이 없을 때까지 반복한다.**
시작 시각이 단조 증가하기만 하므로 앞서 맞춘 안정화·휴지 조건이 다시 깨지지 않는다
(뒤로 갈수록 간격은 넓어지기만 한다). 밀 때마다 어떤 구간의 끝을 넘어서므로 반드시 끝난다.

**D18. 스케줄 실패도 `Violation` 타입을 그대로 쓴다.** 위반 코드와 출력 형식을 검증기와 공유하므로
CLI에 출력 경로가 하나만 있으면 된다. "배치 불가 사유"와 "제약 위반"은 결국 같은 것을 가리킨다.

**D19. 순환 검출을 `model/dependency_graph`로 빼내 검증기와 스케줄러가 공유한다.**
같은 판정을 두 곳에서 다르게 구현하면 언젠가 어긋난다.

**D20. `ess schedule`은 배치 결과를 그대로 검증기에 다시 넣어 확인한다.**
스케줄러와 검증기는 별개 구현이라 어긋날 수 있다. 자체 검증이 실패하면 그건 사용자 입력 문제가
아니라 버그이므로, 종료 코드 `3`(내부 오류)으로 구분해 보고한다.

---

## 리뷰 반영

**D21. 스케줄러는 사전 조건을 확인하고, 어기면 "입력 오류"로 따로 보고한다.**
참조 무결성은 로더의 책임이지만(D4), 로더를 거치지 않고 `schedule()`을 직접 부르는 경로가 있다.
알 수 없는 자원을 조용히 건너뛰면 **시각이 비어 있는 동작을 남긴 채 성공을 반환**하게 되고,
그 결과를 그대로 쓰는 쪽에서 빈 값을 역참조한다. 성공을 반환하면서 불완전한 출력을 내는 API는
계약이 깨진 것이다. `ScheduleResult`에 `input_errors`를 따로 두어 "제약 때문에 불가능"과
"입력이 잘못됨"을 구분했고, CLI는 후자를 종료 코드 `2`로 보고한다.

**D22. `ScheduleView`의 내부 색인은 포인터가 아니라 인덱스를 담는다.**
`placed_` 원소의 주소를 담으면 뷰를 복사했을 때 사본의 색인이 원본 벡터를 가리켜, 원본이 사라지는
순간 매달린 포인터가 된다. 컴파일러가 막아주지 않는 함정이라 자료구조 쪽에서 없앴다.
복사해도 안전한지 확인하는 테스트를 함께 두었다.

**D23. 로더는 한 필드에 오류를 겹쳐 내지 않는다.**
읽기에 실패한 값을 기본값으로 대체한 뒤 그 기본값을 다시 검사하면, 같은 필드에
"음수는 쓸 수 없습니다"와 "1 이상이어야 합니다"가 나란히 붙어 서로 모순돼 보인다.
값을 읽은 결과가 있을 때만 범위를 검사하도록 바꿨다.

---

## C++20 기능 사용처

| 기능 | 사용 위치 | 이유 |
|------|-----------|------|
| `std::span` | `ScheduleView::placed()`, `on_resource()`, `main()`의 argv | 소유권 없이 연속 구간을 넘기면서 크기 정보를 함께 들고 다닌다 |
| Concepts | `ValidationRule` (`src/validate/rules.hpp`) | 규칙이 갖춰야 할 형태를 컴파일 타임에 강제 (D5) |
| `<=>` | `Interval`, `PlacedStep` (`src/model/types.hpp`) | 정렬 기준을 한 곳에 모아 결정론적 순서를 보장 (D6) |
| Designated initializer | 모델 생성 전반, 특히 테스트 시나리오 구성 | 필드가 많은 설정 구조체에서 무엇을 설정했는지 읽히게 한다 |
| `std::format` | 로더·규칙·CLI 메시지 | 위치 정보가 섞인 오류 메시지를 안전하게 조립 (D12) |
| `contains()` | 로더의 참조 무결성 검사 | `find() != end()`보다 의도가 그대로 읽힌다 |
| Ranges 알고리즘 | 순환 탐색·배타 목록 정리·배치 결과 정렬 | `begin()/end()` 쌍을 넘기지 않아 정렬 대상이 한눈에 보인다 |
